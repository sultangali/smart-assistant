# 📝 Заметки по деплою

## IP адрес сервера

**IP адрес сервера по умолчанию: `34.88.173.3`**

Все скрипты деплоя настроены на использование этого IP адреса:
- Если автоматическое определение IP не удаётся, используется `34.88.173.3`
- Nginx конфигурация использует этот IP в `server_name`
- CORS_ORIGIN в `.env` файле использует этот IP
- SSL сертификат (самоподписанный) создаётся для этого IP

## Что НЕ меняется

- **MongoDB URI**: остаётся `mongodb://localhost:27017/smart-assistant` (локальное соединение)
- **Nginx upstream**: остаётся `127.0.0.1:5000` (внутренний прокси для Node.js)

## Быстрый деплой

```bash
# На VPS сервере
sudo ./05-full-deploy.sh
```

Скрипт автоматически:
1. Определит IP адрес (или использует 34.88.173.3)
2. Установит все зависимости
3. Настроит Nginx с правильным IP
4. Настроит SSL сертификат для IP
5. Запустит приложение

## После получения домена

Когда у вас будет доменное имя:

```bash
sudo ./06-update-domain.sh
```

Это обновит:
- Nginx конфигурацию
- SSL сертификат (Let's Encrypt)
- CORS_ORIGIN в .env




