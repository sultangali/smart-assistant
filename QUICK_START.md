# ⚡ Быстрый старт

## 🚀 За 5 минут до запуска

### 1. Установка зависимостей

```bash
# Backend
cd server
npm install

# Frontend
cd ../client
npm install
```

### 2. Настройка .env

Создайте файл `server/.env`:

```bash
cd server
cp env.example .env
```

Минимальная настройка для начала работы:

```env
NODE_ENV=development
PORT=5000
MONGODB_URI=mongodb://localhost:27017/smart-assistant
JWT_SECRET=your-secret-key-change-this
ADMIN_EMAIL=admin
ADMIN_PASSWORD=admin123!
CORS_ORIGIN=http://localhost:5173
```

### 3. Инициализация базы данных

```bash
cd server
npm run seed
```

Это создаст:
- ✅ Администратора
- ✅ Инструменты
- ✅ Весь контент с переводами

### 4. Запуск

**Терминал 1 (Backend):**
```bash
cd server
npm run dev
```

**Терминал 2 (Frontend):**
```bash
cd client
npm run dev
```

### 5. Вход в админ-панель

Откройте: `http://localhost:5173/admin/login`

- **Email:** `admin` (или из `.env`)
- **Password:** `admin123!` (или из `.env`)

---

## 📧 Настройка Email (опционально)

Для работы ротации паролей настройте SMTP в `server/.env`:

```env
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
```

**Без SMTP:** Пароли будут выводиться в консоль сервера (только в development).

---

## ✅ Готово!

Теперь у вас:
- ✅ Работающий backend на `http://localhost:5000`
- ✅ Работающий frontend на `http://localhost:5173`
- ✅ База данных с данными
- ✅ Админ-панель для управления контентом
- ✅ Автоматическая ротация паролей (если настроен SMTP)
- ✅ Полная защита от атак

---

## 🔧 Полезные команды

```bash
# Инициализация БД
npm run seed

# Только инструменты
npm run seed:tools

# Только контент
npm run seed:content

# Проверка API переводов
npm run test-i18n
```

---

## 📚 Дополнительная документация

- [PRODUCTION_SETUP.md](./PRODUCTION_SETUP.md) - Полная настройка для production
- [README.md](./README.md) - Общая информация о проекте

---

**Версия:** 2.0.0



