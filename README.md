# Smart Assistant 1.0

Профессиональный лендинг-сайт с админ-панелью для каталога ИИ-инструментов в образовании.

## 🚀 Технологический стек

### Frontend
- **React 18** + **Vite** - современная и быстрая разработка
- **React Bootstrap** - адаптивный UI-фреймворк
- **React Router** - маршрутизация
- **i18next** - интернационализация
- **Axios** - HTTP-клиент

### Backend
- **Node.js** + **Express** - серверная часть
- **MongoDB** + **Mongoose** - база данных
- **JWT** - аутентификация
- **bcryptjs** - хеширование паролей
- **Joi** - валидация данных

### Безопасность
- **Helmet** - безопасность HTTP-заголовков
- **CORS** - контроль доступа
- **Rate Limiting** - защита от DoS-атак

## 📁 Структура проекта

```
Smart Assistant 1.0/
├── client/                 # React приложение
│   ├── src/
│   │   ├── components/     # React компоненты
│   │   ├── pages/          # Страницы
│   │   ├── contexts/       # React контексты
│   │   ├── utils/          # Утилиты
│   │   ├── i18n/           # Переводы
│   │   └── styles/         # Стили
│   ├── package.json
│   └── vite.config.js
├── server/                 # Express сервер
│   ├── controllers/        # Контроллеры
│   ├── routes/            # Роуты API
│   ├── middleware/        # Middleware
│   ├── config/            # Конфигурация
│   ├── db/                # База данных
│   │   └── models/        # Mongoose модели
│   ├── package.json
│   └── index.js
└── README.md
```

## 🛠 Установка и запуск

### Предварительные требования
- Node.js (версия 18 или выше)
- MongoDB (локальная установка или облачная)
- npm или yarn

### 1. Клонирование и установка зависимостей
```bash
# Установка зависимостей для клиента
cd client
npm install

# Установка зависимостей для сервера
cd ../server
npm install
```

### 2. Настройка переменных окружения
Создайте файл `.env` в папке `server/` со следующими параметрами:

```env
NODE_ENV=development
PORT=5000
MONGODB_URI=mongodb://localhost:27017/smart-assistant
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRE=7d
ADMIN_EMAIL=admin
ADMIN_PASSWORD=admin123!
CORS_ORIGIN=http://localhost:5173
```

### 3. Запуск в режиме разработки
```bash
# Запуск сервера (в одном терминале)
cd server
npm run dev

# Запуск клиента (в другом терминале)
cd client
npm run dev
```

### 4. Доступ к приложению
- **Лендинг:** http://localhost:5173
- **API:** http://localhost:5000/api
- **Админ-панель:** http://localhost:5173/admin

## 🔐 Учетные данные администратора

По умолчанию создается администратор с учетными данными:
- **Email:** admin
- **Пароль:** admin123!

## 📖 Функциональность

### Лендинг-страница
1. **Hero секция** - главный заголовок с фоновыми линиями
2. **О проекте** - описание платформы
3. **Фильтр инструментов** - поиск по 3 критериям
4. **Обратная связь** - форма для жалоб и предложений
5. **Контакты** - контактная информация

### Система фильтрации
- Поиск по категориям
- Зависимые подкатегории
- Дополнительная фильтрация по назначению
- Отображение результатов карточками

### Админ-панель
- JWT-аутентификация
- CRUD операции с инструментами
- Управление категориями и подкатегориями
- Настройка цветов карточек
- Просмотр и обработка обратной связи

## 🎨 Дизайн

### Цветовая палитра
- `#03045E` - основной темный
- `#312A57` - вторичный темный
- `#8182AF` - средний
- `#E0E0EB` - светлый фон
- `#FDC280` - акцент оранжевый
- `#FB8500` - яркий акцент

### Принципы дизайна
- Модерный и минималистичный стиль
- Чередующиеся светлые и темные секции
- Полноэкранные секции (100vh)
- Адаптивный дизайн
- Плавные анимации и переходы

## 🔧 Скрипты разработки

```bash
# Установка зависимостей
cd client && npm install
cd server && npm install

# Разработка
cd server && npm run dev     # Сервер
cd client && npm run dev     # Клиент

# Продакшн
cd client && npm run build   # Сборка клиента
cd server && npm start       # Запуск сервера

# Качество кода (для каждой папки отдельно)
cd client && npm run lint && npm run format
cd server && npm run lint && npm run format
```

## 📊 API Endpoints

### Публичные
- `GET /api/tools` - получить инструменты с фильтрацией
- `GET /api/tools/categories` - получить категории
- `POST /api/feedback` - отправить обратную связь

### Админские (требуют аутентификации)
- `POST /api/auth/login` - авторизация
- `GET /api/auth/profile` - профиль админа
- `POST /api/tools` - создать инструмент
- `PUT /api/tools/:id` - обновить инструмент
- `DELETE /api/tools/:id` - удалить инструмент
- `GET /api/feedback` - получить обратную связь
- `PUT /api/feedback/:id` - обновить статус обратной связи

## 🔒 Безопасность

- Хеширование паролей с bcrypt
- JWT токены для аутентификации
- Rate limiting (100 запросов/15 минут)
- Helmet для безопасности заголовков
- CORS настройка
- Валидация данных с Joi

## 🌐 Интернационализация

Поддержка русского и английского языков через i18next:
- Автоматическое определение языка
- Переключение языка
- Сохранение выбора в localStorage

## 📱 Адаптивность

- Mobile-first подход
- Bootstrap адаптивная сетка
- Оптимизация для всех устройств
- Touch-friendly интерфейс

## 🚀 Деплой

### Подготовка к продакшену
1. Смените JWT_SECRET на случайную строку
2. Обновите пароль администратора
3. Настройте продакшн MongoDB
4. Соберите клиент: `npm run client:build`

### Переменные окружения для продакшена
```env
NODE_ENV=production
PORT=5000
MONGODB_URI=mongodb+srv://user:pass@cluster.mongodb.net/smart-assistant
JWT_SECRET=your-production-secret-key
ADMIN_EMAIL=your-admin@email.com
ADMIN_PASSWORD=SecureProductionPassword
CORS_ORIGIN=https://yourdomain.com
```

## 🤝 Поддержка

Для вопросов и поддержки обращайтесь к команде разработки.

## 📄 Лицензия

MIT License
