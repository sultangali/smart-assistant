# 🔧 Исправление проблемы с категориями

## 🔍 Проблема

Инструменты были сохранены с **полными русскими названиями** категорий и подкатегорий:
- ❌ `category: 'Персонализированное обучение'`
- ❌ `subcategory: 'Адаптивное обучение'`

Но фильтрация на фронтенде ищет по **английским ключам**:
- ✅ `category: 'personalized_learning'`
- ✅ `subcategory: 'adaptive_learning'`

Из-за этого несоответствия фильтры не находят инструменты.

---

## ✅ Решение

### Вариант 1: Быстрое исправление существующих данных (РЕКОМЕНДУЕТСЯ)

Запустите скрипт исправления, который заменит русские названия на английские ключи:

```bash
cd /var/www/smart-assistant/server
node db/seeders/fixTools.js
```

**Что делает скрипт:**
1. Находит все инструменты в БД
2. Заменяет полные названия на ключи в полях `category` и `subcategory`
3. Сохраняет переводы в новых полях `categoryName` и `subcategoryName`
4. Обновляет все записи

**Результат:**
```
Было:
  category: 'Персонализированное обучение'
  subcategory: 'Адаптивное обучение'

Стало:
  category: 'personalized_learning'
  subcategory: 'adaptive_learning'
  categoryName: 'Персонализированное обучение'
  subcategoryName: 'Адаптивное обучение'
```

### Вариант 2: Полная перезаливка данных

Если хотите начать с чистого листа:

```bash
cd /var/www/smart-assistant/server
node db/seeders/seedTools.js
```

Этот скрипт:
1. Удалит все старые инструменты
2. Загрузит 139 инструментов с правильными ключами
3. Проверит распределение по категориям

---

## 🚀 После исправления

### 1. Перезапустите сервер

```bash
pm2 restart index
```

### 2. Проверьте в браузере

Откройте сайт и попробуйте фильтры:
- Выберите категорию
- Выберите подкатегорию
- Нажмите "Найти"

Теперь должно работать! ✅

### 3. Проверьте через MongoDB

```bash
mongosh
use smart-assistant
db.tools.findOne()
```

Убедитесь что:
- `category` - это английский ключ (например, `personalized_learning`)
- `subcategory` - это английский ключ (например, `adaptive_learning`)
- `categoryName` - это переведенное название (например, `Персонализированное обучение`)
- `subcategoryName` - это переведенное название (например, `Адаптивное обучение`)

---

## 📊 Ожидаемый результат

После исправления вы должны увидеть:

```
📋 Распределение по категориям:
   - personalized_learning: 19 инструментов
   - interactive_technologies: 19 инструментов
   - tech_platforms: 20 инструментов
   - assessment_feedback: 20 инструментов
   - inclusion_accessibility: 20 инструментов
   - content_creation_category: 19 инструментов
   - learning_support: 19 инструментов
```

А **НЕ**:
```
📋 Распределение по категориям:
   - Персонализированное обучение: 0 инструментов ❌
   - Интерактивные технологии: 0 инструментов ❌
   ...
```

---

## 🔍 Проверка API

После исправления проверьте API:

```bash
# Получить все инструменты
curl http://localhost:5000/api/tools

# Фильтр по категории
curl "http://localhost:5000/api/tools?category=personalized_learning"

# Фильтр по подкатегории
curl "http://localhost:5000/api/tools?subcategory=adaptive_learning"

# Комбинированный фильтр
curl "http://localhost:5000/api/tools?category=personalized_learning&subcategory=adaptive_learning"
```

Все запросы должны возвращать инструменты! ✅

---

## 📝 Что было исправлено

### Файлы:

1. **server/db/seeders/toolsData.js**
   - Исправлена функция `convertToTools()` для сохранения ключей

2. **server/models/Tool.js**
   - Добавлены поля `categoryName` и `subcategoryName` для переводов

3. **server/db/seeders/seedTools.js**
   - Обновлена проверка по категориям

4. **server/db/seeders/fixTools.js** (НОВЫЙ)
   - Скрипт для исправления существующих данных

### Изменения в структуре БД:

```javascript
// СТАРАЯ структура
{
  category: 'Персонализированное обучение',  // ❌ полное название
  subcategory: 'Адаптивное обучение'         // ❌ полное название
}

// НОВАЯ структура
{
  category: 'personalized_learning',         // ✅ ключ для фильтрации
  subcategory: 'adaptive_learning',          // ✅ ключ для фильтрации
  categoryName: 'Персонализированное обучение',    // ✅ для отображения
  subcategoryName: 'Адаптивное обучение'            // ✅ для отображения
}
```

---

## ⚡ Быстрый старт

Выполните эти команды на сервере:

```bash
# 1. Перейти в папку сервера
cd /var/www/smart-assistant/server

# 2. ВАРИАНТ A: Исправить существующие данные
node db/seeders/fixTools.js

# ИЛИ ВАРИАНТ B: Полная перезаливка
node db/seeders/seedTools.js

# 3. Перезапустить сервер
pm2 restart index

# 4. Проверить логи
pm2 logs --lines 50
```

Готово! Фильтры должны заработать! 🎉

---

## 🆘 Если проблема остается

1. **Проверьте контроллер tools**
   ```bash
   cat /var/www/smart-assistant/server/controllers/toolController.js
   ```
   
   Убедитесь что фильтрация идет по полям `category` и `subcategory`

2. **Проверьте данные в БД**
   ```javascript
   mongosh
   use smart-assistant
   
   // Посмотреть один инструмент
   db.tools.findOne()
   
   // Посмотреть все уникальные категории
   db.tools.distinct('category')
   
   // Посмотреть все уникальные подкатегории
   db.tools.distinct('subcategory')
   ```

3. **Проверьте логи сервера**
   ```bash
   pm2 logs --lines 100
   ```

4. **Проверьте консоль браузера**
   - Откройте DevTools (F12)
   - Вкладка Console
   - Посмотрите запросы к API
   - Проверьте параметры фильтрации

---

*Дата: Январь 2026*
