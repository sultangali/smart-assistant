# 🚀 Руководство по настройке для Production

## 📋 Содержание

1. [Требования](#требования)
2. [Установка зависимостей](#установка-зависимостей)
3. [Настройка переменных окружения](#настройка-переменных-окружения)
4. [Инициализация базы данных](#инициализация-базы-данных)
5. [Настройка Email (SMTP)](#настройка-email-smtp)
6. [Безопасность](#безопасность)
7. [Запуск приложения](#запуск-приложения)
8. [Мониторинг и обслуживание](#мониторинг-и-обслуживание)

---

## 📦 Требования

- **Node.js** >= 18.x
- **MongoDB** >= 5.0
- **npm** >= 9.x
- **SMTP сервер** (для отправки email уведомлений)

---

## 🔧 Установка зависимостей

### Backend (Server)

```bash
cd server
npm install
```

### Frontend (Client)

```bash
cd client
npm install
```

---

## ⚙️ Настройка переменных окружения

### 1. Создайте файл `.env` в папке `server/`

Скопируйте `env.example` в `.env`:

```bash
cd server
cp env.example .env
```

### 2. Настройте обязательные переменные

Откройте `.env` и настройте следующие переменные:

```env
# ОСНОВНЫЕ НАСТРОЙКИ
NODE_ENV=production
PORT=5000

# БАЗА ДАННЫХ
MONGODB_URI=mongodb://localhost:27017/smart-assistant
# Или для MongoDB Atlas:
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/smart-assistant

# JWT (ОБЯЗАТЕЛЬНО ИЗМЕНИТЕ!)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-2024
JWT_EXPIRE=7d

# АДМИНИСТРАТОР
ADMIN_EMAIL=admin@yourdomain.com
ADMIN_PASSWORD=SecurePassword123!
ADMIN_NOTIFICATION_EMAIL=admin@yourdomain.com

# РОТАЦИЯ ПАРОЛЕЙ
PASSWORD_ROTATION_DAYS=7

# CORS
CORS_ORIGIN=https://yourdomain.com

# EMAIL (SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM=noreply@yourdomain.com
```

### 3. Настройте переменные для клиента

Создайте `.env` в папке `client/`:

```env
VITE_API_URL=http://localhost:5000/api
# Или для production:
# VITE_API_URL=https://api.yourdomain.com/api
```

---

## 🗄️ Инициализация базы данных

### Запуск сидера

После настройки `.env`, запустите скрипт инициализации:

```bash
cd server
npm run seed
```

Этот скрипт:
- ✅ Создаст администратора (если не существует)
- ✅ Добавит инструменты в базу данных
- ✅ Добавит весь контент с переводами (ru, en, kk)

### Проверка данных

После запуска сидера вы должны увидеть:

```
✅ Добавлено X инструментов
✅ Добавлено Y записей контента
✅ Администратор создан
```

---

## 📧 Настройка Email (SMTP)

### Gmail

1. Включите двухфакторную аутентификацию
2. Создайте пароль приложения:
   - Перейдите в [Настройки аккаунта Google](https://myaccount.google.com/)
   - Безопасность → Пароли приложений
   - Создайте новый пароль приложения
3. Используйте этот пароль в `SMTP_PASS`

```env
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-16-char-app-password
```

### Yandex

```env
SMTP_HOST=smtp.yandex.ru
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=your-email@yandex.ru
SMTP_PASS=your-password
```

### Mail.ru

```env
SMTP_HOST=smtp.mail.ru
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=your-email@mail.ru
SMTP_PASS=your-password
```

### Проверка настройки Email

После настройки SMTP, система автоматически:
- ✅ Отправляет новый пароль при ротации
- ✅ Отправляет предупреждения за 1-2 дня до истечения пароля

---

## 🔒 Безопасность

### ✅ Реализованные меры безопасности:

1. **Helmet.js** - Защита HTTP заголовков
2. **Rate Limiting** - Ограничение количества запросов
3. **CORS** - Настройка разрешенных источников
4. **MongoDB Sanitization** - Защита от NoSQL injection
5. **XSS Protection** - Защита от XSS атак
6. **Input Validation** - Валидация всех входных данных (Joi)
7. **Password Hashing** - Bcrypt с 12 раундами
8. **JWT Authentication** - Безопасная аутентификация
9. **Password Rotation** - Автоматическая смена паролей
10. **Account Locking** - Блокировка после неудачных попыток входа

### 🔐 Рекомендации для Production:

1. **Измените JWT_SECRET** на уникальное значение
2. **Используйте HTTPS** для всех соединений
3. **Настройте firewall** для ограничения доступа
4. **Регулярно обновляйте зависимости**: `npm audit fix`
5. **Используйте переменные окружения** для всех секретов
6. **Настройте мониторинг** и логирование
7. **Регулярно делайте резервные копии** базы данных

---

## 🚀 Запуск приложения

### Development

**Backend:**
```bash
cd server
npm run dev
```

**Frontend:**
```bash
cd client
npm run dev
```

### Production

**Backend (с PM2):**
```bash
cd server
npm install -g pm2
pm2 start index.js --name smart-assistant-api
pm2 save
pm2 startup
```

**Frontend (Build):**
```bash
cd client
npm run build
# Разверните папку dist на вашем веб-сервере (nginx, Apache, etc.)
```

### Docker (опционально)

Создайте `docker-compose.yml`:

```yaml
version: '3.8'
services:
  mongodb:
    image: mongo:7
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db
    environment:
      MONGO_INITDB_DATABASE: smart-assistant

  api:
    build: ./server
    ports:
      - "5000:5000"
    environment:
      - NODE_ENV=production
      - MONGODB_URI=mongodb://mongodb:27017/smart-assistant
    depends_on:
      - mongodb

volumes:
  mongodb_data:
```

---

## 📊 Мониторинг и обслуживание

### Ротация паролей

Система автоматически:
- ✅ Проверяет пароли каждые 6 часов
- ✅ Генерирует новый пароль при истечении срока
- ✅ Отправляет email с новым паролем
- ✅ Отправляет предупреждения за 1-2 дня до истечения

### Ручная ротация пароля

Через API (требуется авторизация):

```bash
POST /api/auth/force-rotate-password
Authorization: Bearer <token>
```

### Изменение настроек уведомлений

```bash
PUT /api/auth/notification-settings
Authorization: Bearer <token>
Content-Type: application/json

{
  "notificationEmail": "new-email@example.com",
  "passwordRotationDays": 14
}
```

### Резервное копирование базы данных

```bash
# MongoDB backup
mongodump --uri="mongodb://localhost:27017/smart-assistant" --out=/backup/$(date +%Y%m%d)

# Восстановление
mongorestore --uri="mongodb://localhost:27017/smart-assistant" /backup/20241208
```

### Логирование

Все важные события логируются в консоль:
- ✅ Успешные/неудачные попытки входа
- ✅ Ротация паролей
- ✅ Ошибки сервера
- ✅ Попытки NoSQL injection

---

## 🐛 Решение проблем

### Проблема: Email не отправляется

**Решение:**
1. Проверьте настройки SMTP в `.env`
2. Убедитесь, что используете правильный пароль приложения (для Gmail)
3. Проверьте логи сервера на наличие ошибок
4. В development режиме пароль будет выведен в консоль

### Проблема: Контент не загружается

**Решение:**
1. Убедитесь, что запустили `npm run seed`
2. Проверьте подключение к MongoDB
3. Проверьте логи сервера

### Проблема: CORS ошибки

**Решение:**
1. Проверьте `CORS_ORIGIN` в `.env`
2. Убедитесь, что URL клиента совпадает с `CORS_ORIGIN`
3. Для нескольких доменов: `CORS_ORIGIN=https://domain1.com,https://domain2.com`

---

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи сервера
2. Убедитесь, что все переменные окружения настроены
3. Проверьте подключение к MongoDB
4. Проверьте настройки SMTP

---

## ✅ Чеклист перед запуском в Production

- [ ] Изменен `JWT_SECRET` на уникальное значение
- [ ] Настроен `MONGODB_URI`
- [ ] Настроен SMTP для отправки email
- [ ] Установлен `ADMIN_EMAIL` и `ADMIN_PASSWORD`
- [ ] Настроен `CORS_ORIGIN`
- [ ] Запущен `npm run seed` для инициализации БД
- [ ] Настроен HTTPS
- [ ] Настроен firewall
- [ ] Настроено резервное копирование БД
- [ ] Настроен мониторинг

---

**Версия:** 2.0.0  
**Последнее обновление:** Декабрь 2024



