# ✅ Чеклист готовности к Production

## 🔧 Настройка окружения

- [ ] Node.js >= 18.x установлен
- [ ] MongoDB >= 5.0 установлен и запущен
- [ ] Все зависимости установлены (`npm install` в `server/` и `client/`)

## 📝 Конфигурация

- [ ] Файл `server/.env` создан из `env.example`
- [ ] `JWT_SECRET` изменен на уникальное значение
- [ ] `MONGODB_URI` настроен правильно
- [ ] `ADMIN_EMAIL` и `ADMIN_PASSWORD` установлены
- [ ] `CORS_ORIGIN` настроен на правильный домен
- [ ] `PASSWORD_ROTATION_DAYS` установлен (по умолчанию 7)
- [ ] SMTP настройки заполнены (для email уведомлений)

## 🗄️ База данных

- [ ] MongoDB запущен и доступен
- [ ] Запущен `npm run seed` в папке `server/`
- [ ] Проверено, что данные загружены:
  - [ ] Инструменты добавлены
  - [ ] Контент добавлен
  - [ ] Администратор создан

## 🔐 Безопасность

- [ ] `JWT_SECRET` не содержит дефолтных значений
- [ ] Все пароли изменены с дефолтных
- [ ] HTTPS настроен (для production)
- [ ] Firewall настроен (для production)
- [ ] Rate limiting работает
- [ ] CORS настроен правильно

## 📧 Email

- [ ] SMTP настройки проверены
- [ ] Тестовое письмо отправлено успешно
- [ ] `ADMIN_NOTIFICATION_EMAIL` установлен

## 🧪 Тестирование

- [ ] Сервер запускается без ошибок
- [ ] Клиент запускается без ошибок
- [ ] Вход в админ-панель работает
- [ ] Управление контентом работает
- [ ] Переводы загружаются правильно
- [ ] API endpoints отвечают корректно

## 📊 Функциональность

- [ ] Все переводы отображаются (ru, en, kk)
- [ ] Контент можно создавать/редактировать/удалять
- [ ] Инструменты отображаются
- [ ] Фильтрация работает
- [ ] Обратная связь работает

## 🔄 Автоматизация

- [ ] Ротация паролей работает (проверка каждые 6 часов)
- [ ] Email уведомления отправляются
- [ ] Блокировка аккаунта работает после 5 неудачных попыток

## 📚 Документация

- [ ] Прочитан `QUICK_START.md`
- [ ] Прочитан `PRODUCTION_SETUP.md`
- [ ] Прочитан `CHANGES_SUMMARY.md`

## 🚀 Production готовность

- [ ] Резервное копирование БД настроено
- [ ] Мониторинг настроен
- [ ] Логирование настроено
- [ ] Процесс-менеджер настроен (PM2, systemd, etc.)
- [ ] Домен и DNS настроены
- [ ] SSL сертификат установлен

---

## 🐛 Проверка проблем

### Если переводы не загружаются:

1. Проверьте, что запущен `npm run seed`
2. Проверьте подключение к MongoDB
3. Проверьте логи сервера
4. Проверьте, что контент есть в БД:
   ```bash
   mongo
   use smart-assistant
   db.contents.count()
   ```

### Если админ-панель пустая:

1. Проверьте авторизацию (токен в localStorage)
2. Проверьте консоль браузера на ошибки
3. Проверьте Network tab в DevTools
4. Проверьте, что API возвращает данные:
   ```bash
   curl http://localhost:5000/api/content -H "Authorization: Bearer YOUR_TOKEN"
   ```

### Если email не отправляется:

1. Проверьте SMTP настройки в `.env`
2. Проверьте логи сервера
3. Для Gmail используйте пароль приложения
4. В development режиме пароль выводится в консоль

### Если пароль не ротируется:

1. Проверьте, что планировщик запущен (логи при старте сервера)
2. Проверьте `PASSWORD_ROTATION_DAYS` в `.env`
3. Проверьте `passwordLastChanged` в БД
4. Можно запустить вручную через API

---

## ✅ Готово!

Если все пункты отмечены, проект готов к production!

---

**Версия:** 2.0.0  
**Дата проверки:** _______________



